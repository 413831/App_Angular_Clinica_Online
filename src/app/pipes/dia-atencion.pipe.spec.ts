import { DiaAtencionPipe } from './dia-atencion.pipe';

describe('DiaAtencionPipe', () => {
  it('create an instance', () => {
    const pipe = new DiaAtencionPipe();
    expect(pipe).toBeTruthy();
  });
});
